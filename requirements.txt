# Core dependencies (required for POC)
fastapi>=0.111.0
uvicorn>=0.30.0
pydantic>=2.7.0
pyyaml>=6.0.1
jinja2>=3.1.4
streamlit>=1.36.0
python-dotenv>=1.0.1
httpx>=0.27.0
rich>=13.7.0
plotly>=5.22.0
cookiecutter>=2.6.0

# Testing
hypothesis>=6.100.0
pytest>=8.0.0
pytest-anyio>=0.0.0

# Ollama LLM integration (requires Ollama running locally)
# Install Ollama: https://ollama.ai/download
# Pull a model: ollama pull llama3
# httpx is already listed above and handles Ollama HTTP API calls
